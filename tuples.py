#lists, used to store a sequence of objects, but tuples are immunable/cannot be changed
numbers = (1, 2, 3, 3)
# numbers[0] = 10
# parenthesis if tuple
# square bracket if list
# you cannot change tuples, no matter what happens